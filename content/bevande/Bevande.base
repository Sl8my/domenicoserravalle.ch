views:
  - type: cards
    name: Table
    filters:
      and:
        - type == "bevanda"
        - file.name != "Template Bevanda"
    order:
      - file.name
      - score
      - created
    sort:
      - property: file.ctime
        direction: DESC
    image: note.img
